name: atendimentos_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  sqflite: ^2.4.2
  image_picker: ^1.2.1
  get_it: ^9.0.5
  injectable: ^2.6.0
  build_runner: ^2.10.2
  injectable_generator: ^2.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

